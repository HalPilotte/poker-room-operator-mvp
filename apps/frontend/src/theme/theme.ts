

import { createTheme } from '@mui/material/styles';

/** Basic MUI theme scaffold for consistent spacing and typography. */
export const theme = createTheme({
  spacing: 8,
  typography: { fontSize: 14 },
});